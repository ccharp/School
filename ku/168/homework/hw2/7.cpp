#include <iostream>

using namespace std;

int main()

// infinite loop adds two to one so x will never equal 12
{
    int x = 1;
while(x != 12)
    {
        cout << x << endl;
        x+=2;
    } 
return 0;
}
