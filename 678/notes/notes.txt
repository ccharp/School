Producer: process that produces information
Consumer: process that consumes the produced imformation
	- note that a consumer cannot exist without a producer

Abstraction Models:
	unbounded-buffer: never had to block. 
	bounded-buffer: fixed buffer size

How do the process communicate?
	- shared memory
	- message passing: pass messages where kernal is intermediate 

Shared memory
